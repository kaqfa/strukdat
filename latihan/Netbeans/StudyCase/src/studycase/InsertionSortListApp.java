package studycase;

public class InsertionSortListApp {
    LinkedList linkList = new LinkedList();
    
    
    // implementasikan logika insertion sort pada fungsi ini
    void insertionList(int data){
        
    }
    
    public static void main(String[] args) {
        InsertionSortListApp app = new InsertionSortListApp();
        
        app.insertionList(15);
        app.insertionList(23);
        app.insertionList(20);
        app.insertionList(16);
        app.insertionList(35);
        app.insertionList(59);
        app.insertionList(28);
        app.insertionList(13);
        app.linkList.printAll();
    }
    
}
